<!-- Short PR description — what changes and why. -->

## What

## Why

## Verification

- [ ] `npm run typecheck` passes
- [ ] `npm test` passes
- [ ] If output / CLI flags changed, README.md `Options` table and
      `CHANGELOG.md` were updated
- [ ] If the chunk splitter rule changed, a fixture in
      `test/search.test.ts` covers the new edge

## Notes for reviewer

<!-- Anything to eyeball: regex tweaks, frontmatter parsing changes,
     hierarchy indentation, etc. -->
